# results

One markdown file per experiment run: config, environment (macOS, llama.cpp build, wired limit, power state), raw numbers (3 runs), median, and the gate decision it informs. Raw dumps go to `results/raw/` (gitignored); summaries are committed.
